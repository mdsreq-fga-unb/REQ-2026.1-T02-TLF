# Backend global prefix is /api/v1. Use your machine LAN IP so a physical device can reach the API.
# Android emulator → host machine: http://10.0.2.2:3000/api/v1
# iOS Simulator → often http://localhost:3000/api/v1 works
EXPO_PUBLIC_API_URL=http://192.168.1.106:3000/api/v1
