import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  outerBar: {
    width: '100%',
    height: 10,
    borderRadius: 10,
    overflow: 'hidden',
  },
  fillBar: {
    height: '100%',
    borderRadius: 10,
  },
})
