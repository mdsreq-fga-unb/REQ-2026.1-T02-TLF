import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  list: {
    width: '100%',
    gap: 12,
  },
  stateText: {
    fontSize: 13,
    textAlign: 'center',
    paddingVertical: 24,
  },
})
