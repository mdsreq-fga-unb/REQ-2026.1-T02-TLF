import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    borderRadius: 5,
    justifyContent: 'flex-start',
    alignItems: 'center',
    textAlign: 'center',
    padding: 20,
    gap: 20,
    width: '100%',
  },
})
