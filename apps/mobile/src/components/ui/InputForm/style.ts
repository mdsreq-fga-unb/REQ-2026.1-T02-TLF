import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  inputForm: {
    flexDirection: 'row',
    width: '100%',
    height: 52,
    borderRadius: 5,
    borderWidth: 1,
  },
  input: {
    width: '75%',
    textAlign: 'left',
    fontSize: 16,
  },
})
