import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    borderRadius: 10,
    justifyContent: 'flex-start',
    alignItems: 'center',
    padding: 20,
    gap: 10,
    width: '90%',
  },
})
